[project]
name = "travelplanner-multiagent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ddgs>=9.9.3",
    "fastapi>=0.124.2",
    "langchain>=1.1.3",
    "langchain-community>=0.4.1",
    "langgraph>=1.0.4",
    "pydantic>=2.12.5",
    "uvicorn>=0.38.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]